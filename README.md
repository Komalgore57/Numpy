# Numpy

DOC :-  https://docs.google.com/document/d/1GDhCSs9gvbTltCI7smJiy93OmNb8jLOK9g647Iwflec/edit?tab=t.0 



# 100 NumPy Coding Questions (Basic to Advanced)

## Beginner Level (1–30)

1. Create a NumPy array containing numbers from 1 to 10.

2. Create a NumPy array of all zeros with length 10.

3. Create a NumPy array of all ones with shape (3, 4).

4. Create an array of even numbers from 2 to 20.

5. Create an array of 10 equally spaced values between 0 and 1.

6. Create a 3×3 identity matrix.

7. Check the shape, size, and dimensions of an array.

8. Convert a Python list into a NumPy array.

9. Change the datatype of an array from integer to float.

10. Create a random array of size 5.

11. Access the third element of an array.

12. Access the last element of an array.

13. Slice the first five elements of an array.

14. Reverse an array using slicing.

15. Extract all even-indexed elements from an array.

16. Replace the first element of an array with 100.

17. Create a 4×4 matrix and access its second row.

18. Extract the third column from a matrix.

19. Find the shape of a 5×5 matrix.

20. Reshape an array of 12 elements into a 3×4 matrix.

21. Perform element-wise addition of two arrays.

22. Perform element-wise subtraction.

23. Multiply two arrays element-wise.

24. Divide two arrays element-wise.

25. Square every element in an array.

26. Find the sum of all elements in an array.

27. Find the mean of an array.

28. Find the maximum and minimum values.

29. Find the index of the largest value.

30. Calculate the standard deviation of an array.

---

## Easy-Intermediate Level (31–60)

31. Create a 5×5 matrix filled with random integers.

32. Find all values greater than 50 in an array.

33. Replace all negative values with zero.

34. Count how many values are greater than 25.

35. Find unique elements in an array.

36. Sort an array in ascending order.

37. Sort an array in descending order.

38. Find the frequency of each unique value.

39. Remove duplicate values from an array.

40. Merge two arrays into one.

41. Stack two arrays vertically.

42. Stack two arrays horizontally.

43. Split an array into 4 equal parts.

44. Flatten a 2D array.

45. Convert a 3×4 matrix into a 1D array.

46. Create a checkerboard pattern matrix.

47. Create a diagonal matrix from an array.

48. Extract the diagonal elements of a matrix.

49. Find row-wise sums of a matrix.

50. Find column-wise sums of a matrix.

51. Find row-wise maximum values.

52. Find column-wise minimum values.

53. Generate 100 random integers between 1 and 100.

54. Set a random seed and generate reproducible numbers.

55. Find all indices where values are greater than 30.

56. Use `np.where()` to replace values greater than 50 with 999.

57. Create a boolean mask for values less than 20.

58. Filter all odd numbers from an array.

59. Count the number of even values.

60. Compute cumulative sum of an array.

---

## Intermediate Level (61–80)

61. Compute cumulative product of an array.

62. Normalize an array between 0 and 1.

63. Standardize an array using z-score normalization.

64. Calculate the dot product of two vectors.

65. Perform matrix multiplication on two matrices.

66. Transpose a matrix.

67. Find the determinant of a matrix.

68. Find the inverse of a matrix.

69. Calculate eigenvalues and eigenvectors.

70. Solve a system of linear equations.

71. Create a 3D NumPy array.

72. Access a specific element in a 3D array.

73. Slice a 3D array.

74. Sum all elements in a 3D array.

75. Find the mean along a specific axis.

76. Swap rows in a matrix.

77. Swap columns in a matrix.

78. Rotate a matrix by 90 degrees.

79. Flip a matrix horizontally.

80. Flip a matrix vertically.

---

## Advanced Level (81–100)

81. Compute pairwise distances between points using NumPy.

82. Implement one-hot encoding using NumPy.

83. Create a moving average function using NumPy.

84. Find all prime numbers up to 100 using NumPy.

85. Implement min-max scaling without sklearn.

86. Compute correlation between two arrays.

87. Compute covariance matrix of a dataset.

88. Create a custom sigmoid activation function using NumPy.

89. Implement ReLU activation using NumPy.

90. Implement softmax using NumPy.

91. Perform Singular Value Decomposition (SVD).

92. Perform QR decomposition.

93. Calculate the pseudo-inverse of a matrix.

94. Implement batch gradient descent for linear regression.

95. Implement logistic regression using only NumPy.

96. Create a convolution operation on a 1D signal.

97. Compute Fast Fourier Transform (FFT).

98. Simulate rolling a die 10,000 times and analyze probabilities.

99. Implement Monte Carlo estimation of π using NumPy.

100. Build a simple neural network (1 hidden layer) using only NumPy.

## Interview-Level Challenges

* Create a 10×10 matrix with random values and replace the maximum value with 0.
* Find the nearest value to a given number in an array.
* Create a matrix where the border elements are 1 and inside elements are 0.
* Compute Euclidean distance between two vectors.
* Find common elements between two arrays.
* Create a sliding window view of an array.
* Find top 5 largest values without sorting the entire array.
* Implement k-means clustering using only NumPy.
* Perform image grayscale conversion using NumPy.
* Implement Principal Component Analysis (PCA) using NumPy.

These questions are structured exactly as you'd encounter them while progressing from **NumPy beginner → intermediate → data science interview → machine learning preparation**.


